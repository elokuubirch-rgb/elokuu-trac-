# 一生足迹 · 功能清单（MVP）

> **定位**：极简的「足迹回顾」工具 —— 不做实时定位记录，只做**导入与回顾**。
> **数据**：全部来自你已有的照片和 CSV 文件；处理与存储全部在**本机**。
> **形态**：iOS 原生 App（SwiftUI）

---

## 一、产品原则

1. **功能极简**：只有 3 个页面 —— 地图 / 统计 / 设置
2. **数据自动**：授权一次、导入即用，无需手动整理
3. **隐私优先**：无账号、无上传、无广告、无后台定位

---

## 二、功能清单

| 优先级 | 模块 | 功能 | 说明 |
| --- | --- | --- | --- |
| P0 | 数据导入 | **照片位置提取** | 授权相册后扫描带 GPS 的照片，提取坐标与拍摄时间（`PHAsset.location` / `creationDate`） |
| P0 | 数据导入 | **受限相册支持** | 兼容 iOS「选中的照片」授权模式，只扫描用户允许的照片 |
| P0 | 数据导入 | **CSV 导入** | 选文件 → 自动识别列 → 手动映射（纬度/经度/时间/地点名）→ 预览 → 导入 |
| P1 | 数据导入 | 增量扫描 | 记住扫描进度，新增照片自动补扫，不全量重复 |
| P1 | 数据导入 | 手动添加 | 长按地图添加单个足迹点（补充没位置信息的照片） |
| P1 | 数据导入 | 去重聚合 | 同一天 ±50m 内的点自动合并为一个 |
| P0 | 足迹地图 | **足迹点渲染** | 全部点显示在地图上，点多时自动聚合为数字气泡 |
| P0 | 足迹地图 | **时间过滤** | 「全部 / 按年」切换显示 |
| P0 | 足迹地图 | **深色底图 + 足迹高亮** | 地图整体深色低对比仅作背景（道路低亮度、POI 隐藏）；走过的路线高亮叠加（线宽 2px、圆头圆角，频次越高越亮，高频可加 Glow） |
| P0 | 足迹地图 | **点/线双模式** | 默认高亮线；可切换为离散小点模式——放大后可见足迹是独立点，常走路线点更密 |
| P0 | 足迹地图 | **足迹主题系统** | Crimson / Arctic / Neon / Sunset / Mono 五套配色可切换（地图底色 + 足迹色联动） |
| P0 | 足迹地图 | **时间轴回放** | 拖动底部时间轴，足迹按时间动态出现/消失（城市道路网络逐渐形成） |
| P1 | 足迹地图 | 时间范围 / 密度过滤 | 按年过滤；仅显示高频路线 |
| P1 | 足迹地图 | 城市名识别 | 可选联网反解城市名（Apple 服务、带缓存）；不联网也能用 |
| P0 | 统计 | **总览卡片** | 总里程 / 城市数 / 道路覆盖率 / 活跃天数 / 首个足迹日期 |
| P0 | 统计 | **年度分布** | 每年足迹量条形图 |
| P1 | 统计 | 常去城市 Top 列表 | |
| P0 | 设置 | 重新扫描 / 导入 CSV 入口 | |
| P0 | 设置 | **导出备份** | 全部足迹导出为 CSV / KML |
| P0 | 设置 | 清空数据 | 二次确认后删除全部数据 |
| P0 | 设置 | 隐私说明 | 本机存储说明 |

---

## 三、数据流程

```
照片：相册授权 → 增量扫描 PHAsset → 提取(坐标, 拍摄时间) → 去重 → 本地存储 → 地图渲染
CSV：选择文件 → 解析 → 列映射界面 → 预览 → 去重 → 本地存储
城市名：可选步骤，批量反解 + 本地缓存，避免重复网络请求
```

**CSV 导入格式示例**（列名可映射，不要求固定顺序）：

```csv
latitude,longitude,time,name
31.2304,121.4737,2021-10-01T09:00:00,上海
30.5728,104.0668,2022-08-12T15:30:00,成都
```

---

## 四、技术方案

| 项 | 选择 | 理由 |
| --- | --- | --- |
| UI | SwiftUI（iOS 17+） | 声明式、代码量少、贴合极简风格 |
| 地图 | MapKit（强制深色外观） | 深色低对比底图 + 上方叠加足迹高亮层；免费原生、无第三方 SDK 与密钥 |
| 相册 | Photos framework | 系统 API，支持受限权限 |
| 存储 | SwiftData | 本地数据库，天然支持按时间查询 |
| 城市反解 | CLGeocoder（可选） | Apple 服务，带缓存 |
| 依赖 | **零第三方依赖** | 极简 + 隐私 |

---

## 五、界面结构

```
① 授权页（首次启动）
┌────────────────────────┐
│ 9:41            ◉ ▮   │
│        ┌────┐         │
│        │ ◎  │         │
│        └────┘         │
│      一生足迹           │
│ 从你的照片里，找回走过的地方 │
│    ┌────────────┐     │
│    │ 授权访问相册 │     │
│    └────────────┘     │
│  之后再说 · 从 CSV 导入  │
│ 数据仅保存在本机 · 无上传 │
└────────────────────────┘

② 足迹地图（首页 · 深色全屏）
┌────────────────────────┐
│ ‹  2026年8月   ● ⚙     │
│ ┌────────────────────┐ │
│ │    深色低亮底图       │ │
│ │   ▸▸▸▸ 高亮足迹线    │ │
│ │   ···· 未走路网低亮   │ │
│ │   （可拖拽缩放）      │ │
│ └────────────────────┘ │
│ ┌────────────────────┐ │
│ │ 1,284KM·37城市·18%  │ │
│ │ ───────●──── 时间轴  │ │
│ │ [全部][2026][2025]  │ │
│ └────────────────────┘ │
│ (拖动时间轴 → 足迹按时间生长) │
└────────────────────────┘

③ 统计
┌────────────────────────┐
│ 9:41            ◉ ▮   │
│ 统计                    │
│ ┌──────┐ ┌──────┐     │
│ │城市 23│ │足迹3247│    │
│ └──────┘ └──────┘     │
│ ┌────────────────────┐ │
│ │ 每年足迹  条形图      │ │
│ └────────────────────┘ │
│ ┌────────────────────┐ │
│ │ 常去的地方           │ │
│ │ 1 上海    512       │ │
│ │ 2 北京    489       │ │
│ └────────────────────┘ │
│ [地图] [统计] [设置]     │
└────────────────────────┘

④ 设置与数据
┌────────────────────────┐
│ 9:41            ◉ ▮   │
│ 设置                    │
│ 数据来源                │
│ ┌────────────────────┐ │
│ │ ◎ 照片位置  3,247 条 │ │
│ │ ▤ CSV 文件  2 个    │ │
│ │ ＋ 手动添加  12 条   │ │
│ └────────────────────┘ │
│ 操作                   │
│ ┌────────────────────┐ │
│ │ ↻ 重新扫描相册       │ │
│ │ ▤ 导入 CSV 文件     │ │
│ │ ↑ 导出备份          │ │
│ └────────────────────┘ │
│ 隐私  🛡 数据仅保存在本机 │
│ 清空所有数据             │
│ 版本 1.0.0              │
└────────────────────────┘
```

> 完整可交互原型见 **`02-原型图.html`**（浏览器打开）。

---

## 六、里程碑

| 阶段 | 内容 | 预估 |
| --- | --- | --- |
| M1 地基 | Xcode 工程 + 数据模型 + 相册扫描 + 地图首版 | 1–2 天 |
| M2 数据 | CSV 导入 + 去重聚合 + 时间过滤 | 1–2 天 |
| M3 成品 | 统计页 + 设置/导出 + 空态引导 + 视觉打磨 | 1–2 天 |

---

## 七、明确不做（保持极简）

- **实时/后台定位记录** —— 原版「一生足迹」的核心功能；本产品聚焦「回顾」而非「记录」（后续可作 P2 扩展）
- 账号体系、云同步、社区分享
- 广告与商业化
- Android / Web 端
