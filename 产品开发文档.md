# Tracé（一生足迹）产品开发文档

> 版本：1.0 · 更新日期：2026-08-16
> 平台：iOS（iPhone）· 部署目标：iOS 26 · 构建：Xcode + Swift

---

## 目录

1. [产品概述](#1-产品概述)
2. [信息架构](#2-信息架构)
3. [技术架构](#3-技术架构)
4. [数据模型](#4-数据模型)
5. [核心模块详解](#5-核心模块详解)
6. [设计细节](#6-设计细节)
7. [交互细节](#7-交互细节)
8. [性能与可靠性](#8-性能与可靠性)
9. [测试与调试](#9-测试与调试)
10. [已知问题与后续规划](#10-已知问题与后续规划)

---

## 1. 产品概述

**Tracé**（中文名：一生足迹）是一款「地理记忆地图」应用：将用户一生的足迹（位置轨迹、带 GPS 的照片、健身记录）沉淀为一张可探索的个人地图。照片是记忆的核心载体，路线是记忆的骨架，地图是承载一切的空间。

**核心主张**：
- **照片优先**：所有聚合标记以照片为视觉主体，绝不退化为纯文字
- **线路优先**：主动记录或健康导入的路线数据绝不被采样/去重丢弃（原则问题）
- **流畅优先**：14 万+ 足迹点、1.6 万+ 照片在真机上零主线程阻塞
- **极简 UI**：深色玻璃拟态，视觉层级为 照片 > 轨迹 > 地点 > 地图

**核心功能**：
- 低功耗后台足迹采集（访问/重大位置变化 → 智能融合入库）
- 系统照片 EXIF 地理绑定导入（16,031 张照片带坐标）
- CSV 足迹导入（带同日 50m 去重）
- HealthKit 健身路线导入（徒步/跑步，dense 模式保留全部点）
- 主动 GPS 轨迹记录
- 7 级缩放聚合地图（省 → 单张照片）
- 徒步照片绑定轨迹（吸附 + 时间插值）
- 区域照片探索（堆叠卡牌 + 随机洗牌 + 同级跳转）
- 纯净模式（沉浸浏览）
- 到访统计（天数、次数、月度分布）

---

## 2. 信息架构

```
MainTabView（自定义底部菜单栏：地图 / 统计 / 设置）
│
├── 地图页 MapScreen ────────────── 核心页面
│   ├── MKMapView + MarkerCanvasView（聚合标记画布）
│   ├── 底部统计面板（正常模式显示，纯净模式隐藏）
│   │   ├── 足迹统计（点/距离/天数/首末日期）
│   │   ├── 月份 Slider（时间过滤）
│   │   ├── 筛选 chips（点/线/照片开关）
│   │   └── 地图类型 Menu（标准/卫星）
│   ├── 照片架预览（底部，可滑动）
│   ├── 轨迹记录控件（主动 GPS）
│   └── 回当前位置按钮
│
├── 照片探索页 PhotoExploreView ──── 模态/全屏
│   ├── 卡堆（前卡 + 3 张背卡叠放）
│   ├── 信息区（地点 + 时间 + 海拔）
│   ├── 进度条 + 右上角「N 张照片」角标 + 洗牌按钮
│   └── 过渡层（区域跳转动画）
│
├── 统计页 StatsScreen ───────────── 年度/月度统计
└── 设置页 SettingsScreen ────────── 主题、地图、数据导入
```

**页面导航**：
- 地图点击空白处 / 点击标记 / 照片架 → 探索页
- 探索页返回 → 地图（保持原缩放与位置）
- 双击底部「地图」tab → 纯净模式（切换）
- 地图点击已定位区域 → 直接进入该区域探索（跳过照片架）

---

## 3. 技术架构

### 3.1 技术栈

| 领域 | 选型 | 理由 |
|------|------|------|
| UI 框架 | SwiftUI + UIKit 混合 | 地图必须走 UIKit（见 3.3） |
| 持久化 | SwiftData（ModelContainer） | 万级记录 CRUD + 后台线程 ModelContext |
| 地图 | MKMapView（UIViewRepresentable） | **iOS 26.0.1 真机上 SwiftUI Map 渲染不可靠**，原生 MKMapView 是唯一可靠路径 |
| 照片框架 | Photos / PHImageManager | EXIF 读取、iCloud 缩略图 |
| 健康数据 | HealthKit | 徒步/跑步路线导入 |
| 定位 | CLLocationManager（低功耗） | 访问 + 重大位置变化 |
| 单元测试 | SwiftPM 本地包（CoreLogic） | 纯逻辑（吸附/聚合/去重）可测 |
| 调试 | MapDebugLog（DEBUG 文件日志） | devicectl 拉取 Documents/map_debug.txt |

### 3.2 分层架构

```
┌─────────────────────────────────────────────────┐
│ 视图层 Views                                    │
│  MainTabView / MapScreen / PhotoExploreView /   │
│  StatsScreen / SettingsScreen / OnboardingView  │
├─────────────────────────────────────────────────┤
│ 状态与协调层                                     │
│  ExploreSession（探索状态机） / SnapshotCache     │
│  MapScreen 快照加载编排 / 相机指令 MapCameraCommand│
├─────────────────────────────────────────────────┤
│ 服务层 Services                                 │
│  FootprintStore（导入/去重/dense）                │
│  PhotoStore（查询/区域） / RegionService（逆地理） │
│  HealthKitService / LocationService             │
│  PhotoThumbnailGenerator（按需缩略图）            │
├─────────────────────────────────────────────────┤
│ 核心逻辑 CoreLogic（SwiftPM，可测试）             │
│  GeoMath（距离/插值/TrailIndex/吸附/平滑）        │
│  PhotoCluster（7 级聚合索引）                     │
│  FootprintStats / DedupeIndex                   │
├─────────────────────────────────────────────────┤
│ 数据层 SwiftData（FootprintPoint / PhotoRecord） │
└─────────────────────────────────────────────────┘
```

**关键原则**：
- 万级数据全量聚合/统计在 `Task.detached` 后台线程执行，主线程零阻塞
- 全局缓存 `SnapshotCache`：启动后首次加载结果全局复用，重建秒开
- SwiftData 模型对象不跨线程传递，后台只传 `String`/值类型
- 核心逻辑（吸附/聚合/统计）独立成 SwiftPM 包，不依赖 UIKit/SwiftData，单元测试直接运行

### 3.3 地图渲染架构（核心难点）

```
MapScreen (SwiftUI)
  └── FootprintMapView (UIViewRepresentable)
        ├── MKMapView（原生渲染：瓦片/折线/相机）
        └── MarkerCanvasView（透明 UIView 覆盖层）
              ├── 足迹点 FootprintDot（频次尺寸 + 光晕）
              └── 照片聚合标记 PhotoCluster（缩略图 + 角标）
```

**为什么不用 MKAnnotationView / SwiftUI Map**：
- iOS 26 的 `MKOverlayRenderer.point(for:)` 返回错误坐标（MKMapPoint 世界坐标从 2^24 变 2^28）
- `mapView.convert(_:toPointTo: mapView)` 对子视图换算错误
- **规避方案**：所有标记用 `mapView.convert(coordinate, toPointTo: canvasSelf)` 逐帧换算屏幕坐标，在 `draw(_:)` 中绘制——UIKit 坐标系 100% 可控

**拖动跟随**：
- `mapViewDidChangeVisibleRegion` 每帧触发画布重绘
- 拖动期间启动 CADisplayLink 每帧重绘（规避 iOS 26 拖动中 region 回调被压缩导致的标记漂移）
- 手势识别器监听 began/changed/ended 精确启停

**数据流（标记更新）**：
```
SwiftUI 状态（clusterMarkers/dots）→ updateUIView → canvas.clusters/dots
  → setNeedsDisplay → draw → convert 实时坐标 → 绘制
```

### 3.4 快照加载流程

```
loadSnapshots（isLoading 互斥）
  ├─ Task.detached(userInitiated)
  │   ├─ fetch 足迹点（SQL 按时间排序——文件顺序乱序会连出蜘蛛网）
  │   ├─ 月份列表 / 统计 / 百分位包围盒（启动视野）
  │   ├─ fetch 照片记录 → 构建 TrailIndex（健康/gps 轨迹点）
  │   ├─ ClusterIndex.build（7 级聚合 + 轨迹吸附 + 吸附统计回调）
  │   └─ 写回 SnapshotCache / UserDefaults 缓存视野
  └─ MainActor：更新状态 → 初始定位 → 绘制
```

**拖动补全**：拖动后若视野超出已加载区域 60% → 触发增量重建，保证任意拖动都有标记。

---

## 4. 数据模型

### 4.1 FootprintPoint（足迹点）

| 字段 | 类型 | 说明 |
|------|------|------|
| timestamp | Date | 时间 |
| latitude / longitude | Double | 坐标 |
| sourceRaw | String | 来源（health/gps/csv/background） |
| altitude | Double | 海拔 |

### 4.2 PhotoRecord（照片地理绑定）

| 字段 | 类型 | 说明 |
|------|------|------|
| localIdentifier | String | 照片库 id（唯一键） |
| latitude / longitude | Double | EXIF GPS |
| timestamp | Date | 拍摄时间 |
| altitude | Double | 海拔 |
| countryName / provinceName / cityName / districtName | String? | 逆地理结果 |
| regionState | Int | 0 未逆地理 / 1 完成 / 2 失败 |
| thumbnailPath | String? | 缩略图路径 |
| thumbState | Int | 0 未生成 / 1 完成 |

### 4.3 缓存层

- **SnapshotCache**（内存）：pointSnapshots / clusterIndex / monthStarts / stats / dataRegion
- **Thumbs 目录**（磁盘）：`Caches/Thumbs/<localIdentifier>.jpg`，按需生成
- **UserDefaults**：`lastRegionLat/Lon/Span`（启动视野）、主题、地图类型、onboarded

---

## 5. 核心模块详解

### 5.1 七级照片聚合（Apple 照片地图逻辑）

```
缩放跨度（纬度）      级别        分桶方式              网格/粒度
> 8°                省         行政区名                —
> 1.2°              市         行政区名                —
> 0.1°              区         0.1° 等比网格            ≈11km
> 0.03°             乡镇       0.05° 网格              ≈5km
> 0.015°            街区       0.02° 网格              ≈2km
> 0.005°            点         0.005° 网格             ≈500m
≤ 0.005°            单张       0.0005° 网格            ≈55m
```

- **区级及以下**：网格分桶 + **吸附坐标**（贴路线），显示名取网格内众数行政区名，`sampleIds` 保存全量照片 id（点击按 id 集合浏览，不走行政区名查询——历史 bug：行政区名查询会漏照片）
- **省/市级**：真实坐标 + 行政区分桶
- **切换逻辑**：`handleRegionChanged` 40% 迟滞（防抖动）+ 3 倍膨胀预判 + 跨级 morphing 动画（0.25s 淡入淡出 + 缩放，Timer 60fps 驱动）
- **可见过滤**：`visible()` 按视野膨胀 30% 过滤，只渲染可见标记
- **到访次数**：`visitDays`（按拍摄日期去重，跨天算不同到访）

### 5.2 轨迹吸附（徒步照片绑定轨迹）

**TrailIndex**：轨迹空间 + 时间索引
- 空间：0.002°（≈220m）网格哈希，3×3 邻居搜索
- 时间：排序后二分查找 + 线性插值
- **2h 间隙保护**：前后轨迹点间隔 > 2 小时视为跨线路间隙，不插值（避免隔天线路间乱定位）

**snapPhotoToTrailResult 规则**（时间优先，保证沿线均匀分布）：

| 情况 | 规则 | 结果 |
|------|------|------|
| 拍摄时间对应轨迹位置 < 50m | 绑定该位置 | exact |
| 拍摄时间对应轨迹位置 50–300m | 吸附到该位置 | snapped |
| 时间位置过远（时钟偏差/跨线路间隙） | 回退空间最近点（≤50m 精确 / ≤300m 吸附） | exact/snapped |
| GPS 无效 | 按拍摄时间插值（间隙保护） | interpolated |
| 超 300m / 无轨迹 | 保持原坐标 | kept |

**为什么时间优先**：空间最近点会把同段线路上多张照片吸附到同一轨迹点 → 坐标重合 → 放大也合并在一个集合。时间优先让照片沿线路按拍摄时刻均匀展开。

**应用范围**：吸附坐标只作用于精细级（区/镇/街/点/单张）分桶；省/市级保持真实位置（不破坏地理记忆）。

### 5.3 轨迹渲染与采样（线路优先级）

- **路线数据绝不采样/去重**：健康/gps 来源全部点保留（`routeSnapshots`）
- 其他数据（csv/background）采样以控制渲染量
- 折线：`MKPolyline` + `MapLineStyle` 样式表（tag 区分历史/实时光晕/实时实线）
- **dense 导入模式**：健康/主动记录跳过 `DedupeIndex`（同日 50m 去重），保证路线连续性
- Catmull-Rom 平滑（平滑段控制：短距不插值、端点不变、中间值有界）

### 5.4 缩略图系统（磁盘写入安全）

- 按需缓存：`Caches/Thumbs/<localID>.jpg`，浏览到才生成
- **不批量预生成**（历史崩溃：diskwrites_resource 超限被杀）
- PHImageManager 异步请求（`isNetworkAccessAllowed = true` 取 iCloud 缩略图）
- `loadingIDs` 防重（历史崩溃：cpu_resource——draw 循环每帧重复 PH 请求）
- 标记缩略图：`thumbPath` 文件优先 → 随机候选 PH 异步取图 → 失败记 `failedIDs`
- 探索页 prefetch 6 张（翻牌零占位）

### 5.5 逆地理（RegionService）

- 常驻逐批：`geocodeNextBatch` 每批 60 条，0.05° 网格去重（同网格只查一次）
- 按数量优先排序；失败退避（限流保护）
- 直辖市规范化：北京市/上海市/天津市/重庆市 cityName 为区名时 → 提升为直辖市名
- 完成后 `NotificationCenter .photoRegionsUpdated` → 地图重载快照 → 聚合标记渐进出现

### 5.6 后台足迹采集

- 低功耗定位（访问 + 重大位置变化）→ `.footprintsCaptured` 通知 → `Task.detached(utility)` 后台融合入库（时间+空间去重）
- 前台位置服务独立（记录/跟随）

### 5.7 探索会话（ExploreSession 状态机）

```
browsing ──滑完──┬─ 照片组会话（isPlaceCollection）→ index 回 0（循环）
                 └─ 区域会话 → transitioning(next) → 加载同级随机区域
                                              └─ 无候选 → done
```

- **照片组会话**（点聚合标记进入）：`loadPhotos(ids:)` **随机洗牌** + 循环浏览 + 手动洗牌按钮
- **区域会话**（行政区进入）：时间排序 + 随机起点 + 滑完自动跳同级（recent 防重复，上限 4）
- **随机起点安全边界**：`clampedStart` 限制在 0…(count-4)，保证首屏 ≥3 张背卡可见（历史 bug：随机到末尾只剩一张卡）

---

## 6. 设计细节

### 6.1 设计语言

| 元素 | 值 |
|------|-----|
| 页面背景 | `#101114` |
| 卡片底 | `#1C1D20` |
| 强调色 | `#FF7A45`（探索页） |
| 主题色 | crimson 等可切换（AppTheme） |
| 文本 | 主白 / 次 `#9CA3AF` |
| 质感 | 玻璃拟态（ultraThinMaterial / 低透明浅色） |
| 圆角 | 卡片 24pt / 标记 15% 等比 |

**视觉层级（弱到强）**：地图底图 → 足迹点 → 轨迹线 → 照片标记 → 交互控件。卫星图叠加暗化蒙层保证层级不被地图颜色淹没。

### 6.2 地图照片标记（统一样式）

```
┌──────────────────┐
│                  │
│   照片（圆角方形） │  ← 圆角 = 尺寸 × 15%（等比，缩小不变圆）
│   24→54pt 动态   │     白描边 2.5pt
│                  │
└─────┐            │
   数字角标          │  ← 左下角：黑色半透明胶囊 + 白色数字
   └───────────────┘
```

- **大小随缩放连续变化**：`markerSize(for:)` 对数插值，8°（全国）24pt → 0.001°（最近）54pt
- **底板**：极淡毛玻璃（white 6%）+ 细白描边（12%）——几乎不可感知，仅弱化地图干扰（用户要求"尽量感受不到"）
- **角标**：≥2 张照片时显示数量（黑底 55% 透明胶囊、白粗体数字）
- 跨级切换 morphing：旧标记收缩淡出（1→0.75 缩放）+ 新标记放大淡入
- 无黑色投影（用户明确要求去除）

### 6.3 探索页（堆叠卡牌）

- 卡堆：前卡 + 3 张背卡，每张独立随机姿态（FNV-1a 哈希抖动：±22pt 位移、±8° 旋转、±5% 缩放——**确定性随机**，位置稳定不闪烁）
- 背卡层级：scale 递减 + blur 递增（1.8/张）+ 偏移下沉 12pt/张
- 照片 `aspectFit` + 四周留白（设计稿要求，不裁剪照片）
- 卡片底极淡（white 5%），白描边（前 12% / 背 8%），**无阴影**
- 信息区独立于卡片下方：地点白字 + 时间灰字 + 海拔
- 顶部：返回键 + 区域名 + 「N 张照片」强调色角标 + 洗牌按钮（照片组会话）
- 底部：进度条 + 手势提示文案（随会话类型变化）

### 6.4 自定义底部菜单栏

- 毛玻璃底（ultraThinMaterial）+ 顶部 0.5pt 白色细分隔线
- 图标 + 文字竖排，选中态主题色、未选中 secondary
- **正常/纯净模式样式完全一致**（系统 tab bar 已隐藏，`.toolbar(.hidden, for: .tabBar)`）

### 6.5 足迹点样式

- 频次尺寸：4 级（9/7/5.5/4pt），高频光晕（1.1× 半径，18% 透明度）
- 主题色，随统计聚焦

### 6.6 字体与动效

- 数字/统计：rounded heavy 设计
- 动效统一 easeOut 0.25s；翻卡 spring（0.38/0.82、0.24/0.92）

---

## 7. 交互细节

### 7.1 手势体系

| 手势 | 区域 | 行为 |
|------|------|------|
| 单指拖动 | 地图 | 平移地图；标记 CADisplayLink 实时跟随 |
| 双指缩放 | 地图 | 缩放；级别切换（40% 迟滞）+ morphing |
| 单指点击 | 地图空白 | 逆地理命中最近区域 → 探索入口（跳过照片架） |
| 单指点击 | 照片标记 | 打开该组照片探索（ids 集合，随机洗牌） |
| 双击 | 底部「地图」tab | 切换纯净模式 |
| 左/右滑 | 探索页卡片 | 翻牌（速度投影：快速轻甩小位移也能翻） |
| 上/下滑 | 探索页卡片 | 不拦截（方向锁定，垂直交给 sheet） |

### 7.2 纯净模式

- 双击底部「地图」tab 切换
- 隐藏：统计面板、筛选 chips、照片架、记录控件、toast、回当前位置按钮
- **保留**：地图全部图层（点/线/照片标记）+ 底部菜单栏（样式不变）
- 动画：easeOut 0.25s

### 7.3 探索页翻卡细节

- 方向判定一次锁定（水平 ≥ 垂直 × 0.8）
- 飞出阈值：位移 > 屏宽 26% 或速度投影 > 1.4×
- 动画完成后才切 index（消除半途跳变）
- 越界行为：照片组循环 / 区域会话跳同级（过渡层显示下一区域名 + 进度）

### 7.4 地图→探索的镜头衔接

- 点击标记/区域 → 相机动画移动到区域质心（district 8km / city 50km / province 380km）
- 返回时保持地图原缩放与位置

---

## 8. 性能与可靠性

### 8.1 数据规模（真机实测）

- 足迹点：134,591 条
- 照片：16,031 条（全部逆地理完成）
- 聚合：省 16 / 市 60 / 区 297 / 乡镇 455 / 街区 862 / 点 1,925 / 单张 4,856

### 8.2 性能策略

| 问题 | 方案 |
|------|------|
| 万级数据主线程阻塞 | Task.detached 后台全量加载 + 主线程仅状态更新 |
| 拖动卡顿 | 拖动零重建 + 增量图层更新 + 60% 视野补全 |
| 缩略图磁盘写入超限被杀 | 按需生成（禁批量预生成） |
| 每帧重复 PH 请求（cpu_resource 崩溃） | loadingIDs 防重 |
| 缩略图缓存缺失 | 文件 → PH 异步 → 随机候选顶替 |
| 缩略图重复磁盘 IO | 不逐条 fileExists，缺失走 PH 后备 |
| 标记过多 | visible() 视野 30% 膨胀过滤 |

### 8.3 关键崩溃历史与规避（经验沉淀）

1. **cpu_resource**：draw 循环中每帧 PH 请求 → loadingIDs 防重
2. **diskwrites_resource**：批量缩略图生成 → 改为按需缓存
3. **iOS 26 坐标 bug**：`point(for:)` 错误 / `toPointTo: mapView` 错误 → 统一 `toPointTo: self`（canvas）自算

### 8.4 调试设施

- `MapDebugLog`：DEBUG 构建写入 `Documents/map_debug.txt`
  - 快照就绪统计（聚合各层数量/吸附分类统计）
  - canvas 绘制诊断（点/标记数、frame）
  - 级别切换、相机指令日志
- 提取：`xcrun devicectl device copy from --domain-type appDataContainer --domain-identifier com.footprints.LifeFootprints`

---

## 9. 测试与调试

- **单元测试**：`Tests/main.swift` + CoreLogic SwiftPM 包，`swift run FootprintTests`，52 项检查全部通过
  - 轨迹吸附：精确/吸附/插值/间隙回退/沿线时间分布（不堆叠）
  - TrailIndex：最近点搜索/时间插值/2h 间隙保护
  - 轨迹平滑：Catmull-Rom 端点不变/短距不插值/中间值有界
  - 去重、统计、网格 key 精度
- **真机测试**：iPhone 15 Pro Max（iOS 26.0.1），devicectl 装机 + 日志验证
- **模拟器**：iPhone 17（瓦片服务有时异常——历史排查过地图黑屏问题）

---

## 10. 已知问题与后续规划

### 10.1 当前状态

- 吸附统计（真机）：精确 5,589 / 吸附 3,494 / 插值 0 / 保持 6,948（57% 照片绑定到线路）
- 单张级网格 55m：放大沿线照片逐个展开 ✓

### 10.2 后续规划（方案批次）

1. 停留点热力：家/公司圆形节点 + 停留天数
2. 单张照片信息增强：徒步距离（绑定轨迹后按拍摄点计算）
3. 场景动态背景（探索页）
4. 省级数字圆点 / 照片圆堆等 Apple 样式探索（已尝试，当前回退统一缩略图）

### 10.3 设计决策记录（避免回退反复）

| 决策 | 最终状态 | 历史教训 |
|------|----------|----------|
| 省级样式 | 统一缩略图（照片优先） | 文字卡被否、Apple 圆点被否 |
| 标记圆角 | 15% 等比 | 固定 14pt 在缩小后成圆形 |
| 标记大小 | 随缩放连续变化（24→54pt） | 固定大小无层级感 |
| 标记底板 | white 6% 极淡 | 深色底/黑投影被否、16% 太明显 |
| 探索页卡片 | 极淡底 + 无阴影 | 黑色阴影被否 |
| 分组预览 | 单张缩略图 | 3 张横排被否 |
| 组内浏览 | 循环 + 随机洗牌 | 自动跳走被否 |
| 底部菜单栏 | 纯净模式保留、样式一致 | 隐藏后露出系统 tab bar |
